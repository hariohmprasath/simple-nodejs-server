version: 1.0
runtime: nodejs12

build:
  commands:
    build:
      - npm install --loglevel warn

run:
  runtime-version: 12.18.4
  command: node .
  network:
    port: 1337
    env: APP_PORT
  env:
    - name: MY_VAR_EXAMPLE
      value: "example"